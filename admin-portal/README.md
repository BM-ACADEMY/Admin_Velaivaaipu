# Admin Portal

Standalone frontend for the platform admin and sub-admin panels. It uses the existing job portal backend and does not contain a server.

## Setup

1. Copy `.env.example` to `.env` and set the backend URLs.
2. Run `npm install`.
3. Run `npm run dev`.

The default development URL is `http://localhost:5173/login`.
